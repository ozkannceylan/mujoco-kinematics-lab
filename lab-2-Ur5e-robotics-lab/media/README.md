# Lab 2 Media

This folder is reserved for rendered videos and images for the UR5e lab.

The current repository snapshot creates the simulation logs, metrics CSV files,
and documentation first. Once `mujoco` and `matplotlib` are installed, the next
step is to add rendered demos here for:

- pick and place,
- multi-waypoint tour,
- circle tracking,
- singularity stress test.
